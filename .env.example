DATABASE_URL="postgres://postgres:postgres@localhost:51214/template1?sslmode=disable"
# Optional: enables AI card scanning in the admin panel
ANTHROPIC_API_KEY=""

# Admin login (protects /admin and its APIs). Change these before deploying!
AUTH_USERNAME="admin"
AUTH_PASSWORD="hunter2"
